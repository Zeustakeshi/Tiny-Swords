// Inherit the parent event
event_inherited();

max_health = 2000
current_health = max_health
value = 100

status_sprites = [
    spr_tree_uncollected,  // status == 0
	spr_tree_collecting, // status == 1
	spr_tree_collected   // status == 2
];